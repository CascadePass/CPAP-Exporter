<UserControl x:Class="CascadePass.CPAPExporter.OpenFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CascadePass.CPAPExporter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <CheckBox IsChecked="True">
            <TextBlock
                Text="Generate FL and FR events"
                FontWeight="Bold"
                />
        </CheckBox>

        <Border BorderBrush="LightGray" BorderThickness="0.5" CornerRadius="4">
            <Button BorderBrush="Transparent" Background="Transparent" Command="{Binding BrowseCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="pack://application:,,,/Images/FolderOpenBlue.png" Style="{StaticResource NavigationButtonImage}" />

                    <TextBlock
                        Text="{x:Static local:Resources.Browse}"
                        VerticalAlignment="Center"
                        />
                </StackPanel>
            </Button>
        </Border>

        <ItemsControl ItemsSource="{Binding RecentlyUsedFolders}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
