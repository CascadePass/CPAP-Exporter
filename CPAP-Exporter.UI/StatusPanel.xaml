<UserControl x:Class="CascadePass.CPAPExporter.StatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CascadePass.CPAPExporter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Root">

    <Border
        x:Name="StatusPanelBorder"
        Visibility="{Binding StatusMessage, ElementName=Root, Converter={StaticResource NullableObjectToVisibilityConverter}}"
        Background="{Binding StatusBackgroundBrush, ElementName=Root}"
        BorderBrush="{Binding BorderBrush, ElementName=Root}"
        BorderThickness="1"
        CornerRadius="{Binding CornerRadius, ElementName=Root}"
        Padding="8"
        Margin="0,0,0,4">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ShowShadow, ElementName=Root}" Value="True">
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.5" />
                            </Setter.Value>
                        </Setter>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <ContentPresenter
            Content="{Binding StatusMessage, ElementName=Root}" />
    </Border>
</UserControl>
